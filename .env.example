# ============================================
# Synops Labs Dashboard - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!

# ============================================
# API Configuration
# ============================================
# Backend API base URL
# Development: http://localhost:8000
# Production: https://your-backend-api.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================
# Google Sheets Integration (REQUIRED)
# ============================================
# Used for fetching business metrics data
# See docs/GOOGLE_SHEETS_SETUP.md for setup instructions

# Service account email from Google Cloud Console
# Format: your-service-account@project-name.iam.gserviceaccount.com
GOOGLE_SHEETS_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com

# Private key from service account JSON file
# IMPORTANT: Keep the -----BEGIN/END PRIVATE KEY----- markers
# For Vercel: Replace actual newlines with \n
GOOGLE_SHEETS_PRIVATE_KEY=your-private-key-here

# Your Google Sheets spreadsheet ID
# Found in the URL: https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
GOOGLE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id

# ============================================
# OpenAI API (REQUIRED for Alfred AI)
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
# Used for Alfred AI assistant functionality
OPENAI_API_KEY=sk-your-openai-api-key

# ============================================
# Authentication Secrets (REQUIRED)
# ============================================
# Generate strong random strings for production
# Use: openssl rand -base64 32

# Secret for signing JWT access tokens
JWT_SECRET=your-jwt-secret-key

# Secret for signing JWT refresh tokens (should be different from JWT_SECRET)
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key

# ============================================
# Discord Integration (OPTIONAL)
# ============================================
# Only needed if using Discord OAuth for authentication
# Get credentials from: https://discord.com/developers/applications

DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret

# Update this URL based on your deployment
# Development: http://localhost:3000/api/auth/discord/callback
# Production: https://your-domain.com/api/auth/discord/callback
DISCORD_REDIRECT_URI=http://localhost:3000/api/auth/discord/callback

# ============================================
# Plane.so Integration (OPTIONAL)
# ============================================
# Only needed if using Plane.so for project management
# Get credentials from: https://plane.so

PLANE_API_KEY=your-plane-api-key
PLANE_WORKSPACE_ID=your-workspace-id

# ============================================
# Google Calendar Integration (OPTIONAL)
# ============================================
# Only needed if using Google Calendar for scheduling
# Get credentials from: https://console.cloud.google.com

GOOGLE_CALENDAR_CLIENT_ID=your-google-calendar-client-id
GOOGLE_CALENDAR_CLIENT_SECRET=your-google-calendar-client-secret

# ============================================
# Additional Configuration (OPTIONAL)
# ============================================

# Node environment (development | production | test)
# NODE_ENV=development

# Enable debug logging (true | false)
# DEBUG=false

# Sentry DSN for error tracking (optional)
# SENTRY_DSN=your-sentry-dsn
